{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Overlay webview: core window/event access only. All engine traffic goes through Rust commands.",
  "windows": ["main"],
  "permissions": ["core:default", "updater:default"]
}
