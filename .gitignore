/target
/captures
AURICLE_BUILD_PROMPTS.md
ui/node_modules
# Built by `npm run build` in ui/; embedded into the binary at compile time.
crates/auricle-server/ui-dist

# Overlay (Tauri v2) build artifacts.
overlay/node_modules
overlay/dist
overlay/vite.log
overlay/src-tauri/target
overlay/src-tauri/gen
# The engine exe copied in as the MSI sidecar (33 MB build artifact).
overlay/src-tauri/binaries

# Updater signing keypair (RELEASING.md) — the private half must never
# be committed; the public half lives in tauri.conf.json.
overlay/updater.key
overlay/updater.key.pub

